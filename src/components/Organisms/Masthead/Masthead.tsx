import styles from "./Masthead.module.css";

export const Masthead = () => (
	<header className={styles.masthead}>
		<div className="container">
			<p>
				<a href="/">HAMMERSPACE</a>
			</p>
		</div>
	</header>
);
